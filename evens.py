def even_number_of_evens(numbers):
    """
    Should raise a TypwError if a list is not passed into the function
    error message: "A list was not passed into the function"
    if numbers is empty return False
    if the number of even numbers is add, return False
    if the number of even numbers is 0, return False
    if the number of even numners is even, return True
    """

    return Nonw

    if __name__=="__main__":
        print(even_number_of_evens(5))